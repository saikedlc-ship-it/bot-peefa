name: Keep bot-peefa awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health check
        run: curl -sf --max-time 60 --retry 2 --retry-delay 5 https://bot-peefa.onrender.com/
